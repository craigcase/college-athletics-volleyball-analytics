'use client';
import { useState } from 'react';
export function CoachesEdgeInput({matches,initialMatchId}:{matches:any[];initialMatchId?:string}){const [answer,setAnswer]=useState<any>(null),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const fd=new FormData(e.currentTarget);const r=await fetch('/api/coaches-edge/query',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({matchId:fd.get('matchId'),question:fd.get('question')})});setAnswer(await r.json());setBusy(false);}return <div className="edge-workspace"><form className="edge-form" onSubmit={submit}><label>Match<select name="matchId" defaultValue={initialMatchId||matches[0]?.id}>{matches.map(m=><option key={m.id} value={m.id}>{new Date(m.scheduledAt).toLocaleDateString()} · {m.opponentName}</option>)}</select></label><label>Ask anything supported by the current evidence<textarea name="question" rows={3} defaultValue="How did we hit against our opponent?" placeholder="How did we hit against Mayville?" required/></label><div className="shortcut-hint">Try @ for a player, # for an opponent, / for filters — broader resolver support comes after this foundation.</div><button className="primary-button" disabled={busy}>{busy?'Checking evidence…':'Ask Coach\'s Edge'}</button></form>{answer&&<section className="edge-answer"><span className="eyebrow">Answer</span><h2>{answer.answer||answer.message||answer.error}</h2>{answer.scope&&<div className="scope-chip">Analyzing: {answer.scope}</div>}{answer.evidence?.length>0&&<><h3>Evidence</h3><div className="evidence-list">{answer.evidence.map((e:any,i:number)=><div key={i}><b>{e.subject.replace('_',' ')}</b><span>{e.metric.replaceAll('_',' ')}</span><strong>{e.metric==='hitting_percentage'?e.value.toFixed(3).replace(/^0/,''):e.value}</strong></div>)}</div></>}{answer.confidence&&<><h3>Confidence</h3><p>{answer.confidence}</p></>}</section>}</div>}
