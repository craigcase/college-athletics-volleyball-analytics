import { headers } from 'next/headers';import { redirect } from 'next/navigation';import { getCurrentUserFromHeaders } from '../../lib/auth/current-user';import { getActiveProgramForUser } from '../../db/repositories/programs';import { listMatches } from '../../db/repositories/schedule';import { AppShell } from '../../components/app-shell';import { UrlImportForm } from '../../components/url-import-form';
export default async function SchedulePage(){const h=await headers(),u=getCurrentUserFromHeaders(h);if(!u)redirect('/setup');const p=await getActiveProgramForUser(u.email);if(!p)redirect('/setup');const matches=await listMatches(p.programId,p.seasonId);return <AppShell program={p}><div className="page-head"><div><span className="eyebrow">Setup · Step 3</span><h1>Schedule</h1><p>The official schedule becomes the season spine. Refreshes surface meaningful changes instead of silently rewriting history.</p></div></div><section className="panel"><UrlImportForm kind="Schedule" endpoint="/api/schedule/import" placeholder="https://school.edu/sports/volleyball/schedule"/></section><section className="panel"><div className="section-head"><h2>{matches.length} matches</h2><a className="primary-button link-button" href="/matches">Go to Matches →</a></div>{matches.length?<div className="schedule-list">{matches.map((m:any)=><article key={m.id}><time>{new Date(m.scheduledAt).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</time><div><strong>{m.opponentName}</strong><span>{m.homeAway}{m.location?` · ${m.location}`:''}</span></div><b>{m.result||'Scheduled'}</b></article>)}</div>:<div className="empty-panel"><h3>No schedule yet</h3><p>Import the official schedule URL. Multiple matches on the same date remain separate canonical matches.</p></div>}</section></AppShell>}
