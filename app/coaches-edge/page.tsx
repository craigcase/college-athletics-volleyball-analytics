import { headers } from 'next/headers';import { redirect } from 'next/navigation';import { getCurrentUserFromHeaders } from '../../lib/auth/current-user';import { getActiveProgramForUser } from '../../db/repositories/programs';import { listMatches } from '../../db/repositories/schedule';import { AppShell } from '../../components/app-shell';import { CoachesEdgeInput } from '../../components/coaches-edge-input';
export default async function CoachesEdgePage({searchParams}:{searchParams:Promise<{matchId?:string}>}){const h=await headers(),u=getCurrentUserFromHeaders(h);if(!u)redirect('/setup');const p=await getActiveProgramForUser(u.email);if(!p)redirect('/setup');const matches=await listMatches(p.programId,p.seasonId);const sp=await searchParams;return <AppShell program={p} current="Coach's Edge"><div className="edge-head"><span className="eyebrow">Evidence-driven assistant coach</span><h1>Coach's Edge</h1><p>Ask the question. The analytics engine supplies the numbers; Coach's Edge explains only what stored evidence supports.</p></div>{matches.length?<CoachesEdgeInput matches={matches} initialMatchId={sp.matchId}/>:<div className="empty-panel"><h3>No matches to analyze yet</h3><p>Import your schedule and match evidence first.</p></div>}</AppShell>}
